// Terraform skeleton for on-prem Proxmox + RKE
terraform {
  required_version = ">= 1.0"
}

# Provider configuration placeholder for Proxmox and any other on-prem resources.
# Fill in provider blocks, credentials, and resources as required.

// provider "proxmox" {
//   pm_api_url = var.proxmox_url
//   pm_user    = var.proxmox_user
//   pm_password = var.proxmox_password
// }

// Example resource placeholder:
// resource "proxmox_vm" "rke_node" { ... }
